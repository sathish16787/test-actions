name: first-workflow
on : [push]
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps: 
      - name: echo command
        run: echo "Hello world"
      - name: machine info
        run: uname -a
      - name: multiple script
        run: |
          which ls
          env
